<template>
  <div class="container">
      <button type="button" 
      :style="{backgroundColor: color}"
      class="btn"
      @click="shopNow(promotion)"     
      >
          <div class="wrapper">
              <span class="title">{{ title }}</span>
              <Button @click="shopNow(title)">Shop Now</Button>
          </div>
          <img :src="image" alt="item image" class="item-image" />
      </button>
  </div>
</template>

<script>
  import Button from './BannerComponent.vue';

  export default {
      components: {
          Button,
      },

      props: {
      title: String,
      url: String,
      color: String,
      buttonColor: String,
      image: String,
      },

      methods:{
          shopNow(title) {
           alert("Let's shop: "+this.title);
          }
      }
  };
</script>

<style scoped>
  .btn{
      display: grid;
      grid-template-columns: 2fr 1fr;
      grid-template-rows: 1fr;

      height: 170px;
      width: 300px;
      border: solid 1px;
      border-radius: 10px;
      padding-left: 20px;
  }

  .wrapper{
      display: grid;
      grid-template-rows: 70% 30%;
      gap: 10px;
      justify-content: center;
      align-self:center;
      padding-left: 10px;
  }

  .title{
      color: #253D4E;
      font-family:cursive;
      font-size: 13px;
      font-weight: bold;
      text-align: left;
  }

  .item-image {
      display: flex;
      align-self: self-end;
      max-width: 150px;
      max-height: 130px;
  }
</style>


